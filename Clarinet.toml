[project]
name = "compute-pool"
authors = ["Alice Nakamoto <alice@computepool.xyz>"]
description = "Decentralized compute resource allocation platform"
telemetry = false
requirements = []
[contracts.compute-pool]
path = "contracts/compute-pool.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
